// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

export class SetSpeed {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SetSpeed {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSetSpeed(bb:flatbuffers.ByteBuffer, obj?:SetSpeed):SetSpeed {
  return (obj || new SetSpeed()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSetSpeed(bb:flatbuffers.ByteBuffer, obj?:SetSpeed):SetSpeed {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SetSpeed()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

scale():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startSetSpeed(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addScale(builder:flatbuffers.Builder, scale:number) {
  builder.addFieldFloat32(0, scale, 0.0);
}

static endSetSpeed(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSetSpeed(builder:flatbuffers.Builder, scale:number):flatbuffers.Offset {
  SetSpeed.startSetSpeed(builder);
  SetSpeed.addScale(builder, scale);
  return SetSpeed.endSetSpeed(builder);
}
}
